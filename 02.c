#include <stdio.h>

int main(void)
{
    //this work as token like this and semicolen insure where line is ending.
    printf
    (
    "Hellow world\n"
    )
    ;
    return 0;
}

// keywords are reserved words in C lang.there are 32 in C.
// auto, double, int, struct, break, else, long, switch, case, enum, register, typedef, char, extern, return, union, const, float, short, unsigned, continus, for, signed, void, default, goto, sizeof, volatile, do, if, static, while.

// identifier is variable or function name. by which we can identify function or variables is identifier.

//punctuation character in not allowed in variable name like "%$#@!". C is case sencitive.

//there is constant in this code which is 0 in retrun.
//hello world is string

// there are symbol like '&' in ("%d", &a)
// you can use white spaces as many as you want.